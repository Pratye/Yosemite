version: '3'
services:
  yosemite-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
    depends_on:
      - redis
      - ollama
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
